<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top w-100">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Rabijan Ragupalan</a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" 
            data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" 
            aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <% if (title == "Home") { %>
            <a class="nav-link active" href="/home"><i class="fa-solid fa-house"></i> Home</a>
          <% } else { %>
            <a class="nav-link" href="/home"><i class="fa-solid fa-house"></i> Home</a>
          <% } %>
        </li>

        <li class="nav-item">
          <% if (title == "About") { %>
            <a class="nav-link active" href="/about"><i class="fa-solid fa-circle-info"></i> About</a>
          <% } else { %>
            <a class="nav-link" href="/about"><i class="fa-solid fa-circle-info"></i> About</a>
          <% } %>
        </li>

        <li class="nav-item">
          <% if (title == "Project") { %>
            <a class="nav-link active" href="/project"><i class="fa-solid fa-briefcase"></i> Projects</a>
          <% } else { %>
            <a class="nav-link" href="/project"><i class="fa-solid fa-briefcase"></i> Projects</a>
          <% } %>
        </li>

        <li class="nav-item">
          <% if (title == "Contact us") { %>
            <a class="nav-link active" href="/contactus"><i class="fa-solid fa-phone-volume"></i> Contact</a>
          <% } else { %>
            <a class="nav-link" href="/contactus"><i class="fa-solid fa-phone-volume"></i> Contact</a>
          <% } %>
        </li>
      </ul>
    </div>
  </div>
</nav>
